name: Rust

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Download Git submodules
      run: git submodule update --init --recursive
    - uses: coq-community/docker-coq-action@v1
      with:
        custom_image: coqorg/coq:8.20-ocaml-4.14-flambda
        custom_script: |
          startGroup "Install dependencies"
            curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
            source "$HOME/.cargo/env"
            rustup toolchain install nightly-2024-12-07-x86_64-unknown-linux-gnu
            cargo --version
            sudo ln -s `which python3` /usr/bin/python
            opam install -y --deps-only CoqOfRust/coq-of-rust.opam
          endGroup
          startGroup "Build"
            sudo chown -R $(whoami) .
            rustup component add rust-src rustc-dev llvm-tools-preview
            cargo build --verbose
          endGroup
          startGroup "Install"
            cargo install --path lib/
          endGroup
          startGroup "Format check"
            cargo fmt --check
          endGroup
          startGroup "Lint"
            cargo clippy --all-targets --all-features -- -D warnings
          endGroup
          startGroup "Translate Rust examples"
            python run_tests.py
          endGroup
          startGroup "Pre-process the smart contracts"
            cd contracts
            python source_to_generated.py
            cd generated
            for dir in * ; do cd $dir ; cargo check ; cd .. ; done
            cd ../..
          endGroup
          # We disable for now this translation as this makes a panic!
          # startGroup "Translate Ink! library"
          #   cd CoqOfRust
          #   ./generate_ink_example.sh
          #   cd ..
          # endGroup
          # Generate markdown with progress on links for instructions
          startGroup "Generate markdown with progress on links for instructions"
            cd CoqOfRust/revm/revm_interpreter/instructions/links
            python count_admitted.py > progress_on_links.md
            cd ../../../../..
          endGroup
          startGroup "Save space"
            cargo clean
          endGroup
          startGroup "Translate the alloc library"
            cd third-party/rust/library/alloc
            cp ../../../../rust-toolchain ./
            time cargo build
            touch src/lib.rs
            time cargo coq-of-rust
            rsync -rcv src/ ../../../../CoqOfRust/alloc/ --include='*/' --include='*.v' --include='*.rs' --exclude='*'
            cd ../../../..
          endGroup
          startGroup "Translate the core library"
            cd third-party/rust/library/core
            cp ../../../../rust-toolchain ./
            time cargo build
            touch src/lib.rs
            export RUST_MIN_STACK=800000000
            time cargo coq-of-rust
            rsync -rcv src/ ../../../../CoqOfRust/core/ --include='*/' --include='*.v' --include='*.rs' --exclude='*'
            cd ../../../..
          endGroup
          startGroup "Save space"
            cd third-party/rust/library/alloc
            cargo clean
            cd ../../../..
            cd third-party/rust/library/core
            cargo clean
            cd ../../../..
          endGroup
          startGroup "Translate Revm"
            cd third-party/revm
            cp ../../rust-toolchain ./
            cd crates
            # We compile each library twice to avoid errors. TODO: investigate why this is needed
            # bytecode
            cd bytecode
            cargo coq-of-rust
            touch src/lib.rs
            cargo coq-of-rust --with-json
            rsync -rcv src/ ../../../../CoqOfRust/revm/revm_bytecode/ --include='*/' --include='*.v' --include='*.rs' --include='*.json' --exclude='*'
            cd ..
            # context/interface
            cd context/interface
            cargo coq-of-rust
            touch src/lib.rs
            cargo coq-of-rust --with-json
            rsync -rcv src/ ../../../../../CoqOfRust/revm/revm_context_interface/ --include='*/' --include='*.v' --include='*.rs' --include='*.json' --exclude='*'
            cd ../..
            # interpreter
            cd interpreter
            cargo coq-of-rust
            touch src/lib.rs
            cargo coq-of-rust --with-json
            rsync -rcv src/ ../../../../CoqOfRust/revm/revm_interpreter/ --include='*/' --include='*.v' --include='*.rs' --include='*.json' --exclude='*'
            cd ..
            # precompile
            cd precompile
            cargo coq-of-rust
            touch src/lib.rs
            cargo coq-of-rust --with-json
            rsync -rcv src/ ../../../../CoqOfRust/revm/revm_precompile/ --include='*/' --include='*.v' --include='*.rs' --include='*.json' --exclude='*'
            cd ..
            # primitives
            cd primitives
            cargo coq-of-rust
            touch src/lib.rs
            cargo coq-of-rust --with-json
            rsync -rcv src/ ../../../../CoqOfRust/revm/revm_primitives/ --include='*/' --include='*.v' --include='*.rs' --include='*.json' --exclude='*'
            cd ..
            # specification
            cd specification
            cargo coq-of-rust
            touch src/lib.rs
            cargo coq-of-rust --with-json
            rsync -rcv src/ ../../../../CoqOfRust/revm/revm_specification/ --include='*/' --include='*.v' --include='*.rs' --include='*.json' --exclude='*'
            cd ..
            cd ../../..
          endGroup
          startGroup "Translate ruint"
            cd third-party/uint
            cp ../../rust-toolchain ./
            cargo coq-of-rust
            touch src/lib.rs
            cargo coq-of-rust
            rsync -rcv src/ ../../CoqOfRust/ruint/ --include='*/' --include='*.v' --include='*.rs' --exclude='*'
            cd ../..
          endGroup
          startGroup "Translate alloy-core"
            cd third-party/alloy-rs-core/crates/primitives
            cp ../../../../rust-toolchain ./
            cargo coq-of-rust
            touch src/lib.rs
            cargo coq-of-rust
            rsync -rcv src/ ../../../../CoqOfRust/alloy_primitives/ --include='*/' --include='*.v' --include='*.rs' --exclude='*'
            cd ../../../..
          endGroup
          startGroup "Translate bytes"
            cd third-party/bytes
            cp ../../rust-toolchain ./
            grep -q workspace Cargo.toml || echo '[workspace]' >> Cargo.toml
            cargo coq-of-rust
            touch src/lib.rs
            cargo coq-of-rust
            rsync -rcv src/ ../../CoqOfRust/bytes/ --include='*/' --include='*.v' --include='*.rs' --exclude='*'
            cd ../..
          endGroup
          startGroup "Translate Move Sui"
            cd third-party/move-sui
            cp ../../rust-toolchain ./
            cd crates
            # move-abstract-stack
            cd move-abstract-stack
            cargo coq-of-rust
            touch src/lib.rs
            cargo coq-of-rust
            rsync -rcv src/ ../../../../CoqOfRust/move_sui/translations/move_abstract_stack/ --include='*/' --include='*.v' --include='*.rs' --exclude='*'
            cd ..
            # move-binary-format
            cd move-binary-format
            cargo coq-of-rust
            touch src/lib.rs
            cargo coq-of-rust
            rsync -rcv src/ ../../../../CoqOfRust/move_sui/translations/move_binary_format/ --include='*/' --include='*.v' --include='*.rs' --exclude='*'
            cd ..
            # move-bytecode-verifier
            cd move-bytecode-verifier
            cargo coq-of-rust
            touch src/lib.rs
            cargo coq-of-rust
            rsync -rcv src/ ../../../../CoqOfRust/move_sui/translations/move_bytecode_verifier/ --include='*/' --include='*.v' --include='*.rs' --exclude='*'
            cd ..
            # move-bytecode-verifier-meter
            cd move-bytecode-verifier-meter
            cargo coq-of-rust
            touch src/lib.rs
            cargo coq-of-rust
            rsync -rcv src/ ../../../../CoqOfRust/move_sui/translations/move_bytecode_verifier_meter/ --include='*/' --include='*.v' --include='*.rs' --exclude='*'
            cd ..
            # move-core-types
            cd move-core-types
            cargo coq-of-rust
            touch src/lib.rs
            cargo coq-of-rust
            rsync -rcv src/ ../../../../CoqOfRust/move_sui/translations/move_core_types/ --include='*/' --include='*.v' --include='*.rs' --exclude='*'
            cd ..
            cd ../../..
          endGroup
          startGroup "Translate Plonky3"
            cd third-party/move-sui
            cp ../../rust-toolchain ./
            cargo coq-of-rust
            touch src/lib.rs
            cargo coq-of-rust
            rsync -rcv ./air/ ../coq-of-rust/CoqOfRust/plonky3/air --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            rsync -rcv ./baby-bear/ ../coq-of-rust/CoqOfRust/plonky3/baby-bear --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            rsync -rcv ./blake3/ ../coq-of-rust/CoqOfRust/plonky3/blake3 --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            rsync -rcv ./blake3-air/ ../coq-of-rust/CoqOfRust/plonky3/blake3-air --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            rsync -rcv ./bn254-fr/ ../coq-of-rust/CoqOfRust/plonky3/bn254-fr --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            rsync -rcv ./challenger/ ../coq-of-rust/CoqOfRust/plonky3/challenger --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            rsync -rcv ./circle/ ../coq-of-rust/CoqOfRust/plonky3/circle --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            rsync -rcv ./commit/ ../coq-of-rust/CoqOfRust/plonky3/commit --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            rsync -rcv ./dft/ ../coq-of-rust/CoqOfRust/plonky3/dft --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            rsync -rcv ./examples/ ../coq-of-rust/CoqOfRust/plonky3/examples --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            rsync -rcv ./field/ ../coq-of-rust/CoqOfRust/plonky3/field --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            rsync -rcv ./field-testing/ ../coq-of-rust/CoqOfRust/plonky3/field-testing --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            rsync -rcv ./fri/ ../coq-of-rust/CoqOfRust/plonky3/fri --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            rsync -rcv ./goldilocks/ ../coq-of-rust/CoqOfRust/plonky3/goldilocks --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            rsync -rcv ./interpolation/ ../coq-of-rust/CoqOfRust/plonky3/interpolation --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            rsync -rcv ./keccak/ ../coq-of-rust/CoqOfRust/plonky3/keccak --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            rsync -rcv ./keccak-air/ ../coq-of-rust/CoqOfRust/plonky3/keccak-air --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            rsync -rcv ./koala-bear/ ../coq-of-rust/CoqOfRust/plonky3/koala-bear --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            rsync -rcv ./matrix/ ../coq-of-rust/CoqOfRust/plonky3/matrix --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            rsync -rcv ./maybe-rayon/ ../coq-of-rust/CoqOfRust/plonky3/maybe-rayon --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            rsync -rcv ./mds/ ../coq-of-rust/CoqOfRust/plonky3/mds --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            rsync -rcv ./merkle-tree/ ../coq-of-rust/CoqOfRust/plonky3/merkle-tree --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            rsync -rcv ./mersenne-31/ ../coq-of-rust/CoqOfRust/plonky3/mersenne-31 --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            rsync -rcv ./monolith/ ../coq-of-rust/CoqOfRust/plonky3/monolith --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            rsync -rcv ./monty-31/ ../coq-of-rust/CoqOfRust/plonky3/monty-31 --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            rsync -rcv ./poseidon/ ../coq-of-rust/CoqOfRust/plonky3/poseidon --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            rsync -rcv ./poseidon2/ ../coq-of-rust/CoqOfRust/plonky3/poseidon2 --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            rsync -rcv ./poseidon2-air/ ../coq-of-rust/CoqOfRust/plonky3/poseidon2-air --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            rsync -rcv ./rescue/ ../coq-of-rust/CoqOfRust/plonky3/rescue --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            rsync -rcv ./sha256/ ../coq-of-rust/CoqOfRust/plonky3/sha256 --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            rsync -rcv ./symmetric/ ../coq-of-rust/CoqOfRust/plonky3/symmetric --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            rsync -rcv ./uni-stark/ ../coq-of-rust/CoqOfRust/plonky3/uni-stark --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            rsync -rcv ./util/ ../coq-of-rust/CoqOfRust/plonky3/util --include='src/*' --include='*.v' --include='*.rs' --exclude='*'
            cd ../..
          endGroup
          startGroup "Generate Rocq files from Python"
            cd CoqOfRust
            make generate
            cd ..
          endGroup
          startGroup "Check that the diff is empty (excluding submodules)"
            git -c color.ui=always diff --exit-code --ignore-submodules=dirty
          endGroup
          startGroup "Compile Coq translations"
            cd CoqOfRust
            python ./make_and_detect_warnings.py
            cd ..
          endGroup
