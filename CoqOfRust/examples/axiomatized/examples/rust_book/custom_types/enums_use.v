(* Generated by coq-of-rust *)
Require Import CoqOfRust.CoqOfRust.

(* Enum Status *)
(* {
  ty_params := [];
  variants :=
    [
      {
        name := "Rich";
        item := Tuple [];
        discriminant := None;
      };
      {
        name := "Poor";
        item := Tuple [];
        discriminant := None;
      }
    ];
} *)

(* Enum Work *)
(* {
  ty_params := [];
  variants :=
    [
      {
        name := "Civilian";
        item := Tuple [];
        discriminant := None;
      };
      {
        name := "Soldier";
        item := Tuple [];
        discriminant := None;
      }
    ];
} *)

Parameter main : (list Ty.t) -> (list Value.t) -> M.
