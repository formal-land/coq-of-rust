(* Generated by coq-of-rust *)
Require Import CoqOfRust.CoqOfRust.

(*
fn some_fn() {
    ()
}
*)
Definition some_fn : M unit := M.pure tt.

(*
fn main() {
    let a: () = some_fn();
    println!("This function returns and you can see this line.")
}
*)
(* #[allow(dead_code)] - function was ignored by the compiler *)
Definition main : M unit :=
  let* a : M.Val unit :=
    let* α0 : unit := diverging_functions_no_info_in_return_type.some_fn in
    M.alloc α0 in
  let* _ : M.Val unit :=
    let* α0 : M.Val (array (ref str.t)) :=
      M.alloc [ mk_str "This function returns and you can see this line.
" ] in
    let* α1 : M.Val (ref (array (ref str.t))) := M.alloc (borrow α0) in
    let* α2 : ref (slice (ref str.t)) :=
      M.read (pointer_coercion "Unsize" α1) in
    let* α3 : core.fmt.Arguments.t := core.fmt.Arguments.t::["new_const"] α2 in
    let* α4 : unit := std.io.stdio._print α3 in
    M.alloc α4 in
  let* α0 : M.Val unit := M.alloc tt in
  M.read α0.
