(* Generated by coq-of-rust *)
Require Import CoqOfRust.CoqOfRust.

Definition add `{H' : State.Trait} (arguments : i32 * i32) : M (H := H') i32 :=
  let '(a, b) := arguments in
  a.["add"] b.

Definition div `{H' : State.Trait} (arguments : i32 * i32) : M (H := H') i32 :=
  let '(a, b) := arguments in
  let* _ :=
    let* α0 := b.["eq"] 0 in
    if (α0 : bool) then
      let* _ := std.panicking.begin_panic "Divide-by-zero error" in
      Pure tt
    else
      Pure tt in
  a.["div"] b.
